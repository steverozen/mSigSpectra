% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transcript_strand.R
\name{add_transcript_strand}
\alias{add_transcript_strand}
\title{Annotate a VCF data frame with transcript strand information}
\usage{
add_transcript_strand(df, ref_genome, trans_ranges = NULL, name_of_vcf = NULL)
}
\arguments{
\item{df}{A VCF as a data frame / data.table with columns \code{CHROM},
\code{POS}, \code{ALT}.}

\item{ref_genome}{A BSgenome object or a character identifier accepted by
\code{\link[=normalize_genome_arg]{normalize_genome_arg()}}.}

\item{trans_ranges}{Optional \code{data.table} of transcript ranges with
columns \code{chrom}, \code{start}, \code{end}, \code{strand}, \code{Ensembl.gene.ID},
\code{gene.symbol}. If \code{NULL}, the shipped table for \code{ref_genome} is used.}

\item{name_of_vcf}{Optional VCF name used in warning/error messages.}
}
\value{
A \code{data.table} with the annotation columns added (left-join
semantics: variants outside any transcript have \code{NA} values).
}
\description{
For each variant, finds overlapping transcripts in \code{trans_ranges} via
\code{\link[GenomicRanges:findOverlaps-methods]{GenomicRanges::findOverlaps()}} with \code{type = "within"}, and appends
columns \code{trans.start.pos}, \code{trans.end.pos}, \code{trans.strand},
\code{trans.Ensembl.gene.ID}, \code{trans.gene.symbol}, plus \code{bothstrand} (TRUE if
the variant falls on transcripts from both strands) and \code{count} (number
of overlapping transcripts).
}
\details{
If \code{trans_ranges} is \code{NULL} and no shipped table is available for the
given \code{ref_genome}, returns \code{df} as a \code{data.table} unchanged (no strand
columns added). If \code{df} has zero rows, it is returned unchanged.
}
