% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_id_vcf.R
\name{annotate_id_vcf}
\alias{annotate_id_vcf}
\title{Annotate an in-memory ID (indel) VCF with sequence context, transcript
strand, and COSMIC / Koh indel categories}
\usage{
annotate_id_vcf(
  vcf,
  ref_genome,
  trans_ranges = NULL,
  name_of_vcf = NULL,
  suppress_discarded_variants_warnings = TRUE,
  explain_indels = 1,
  context_width_multiplier = 20L,
  add_transcript_ranges = TRUE
)
}
\arguments{
\item{vcf}{An in-memory ID VCF as a \code{data.frame} / \code{data.table}. Expects
a "context base" to the left of the indel, e.g. \code{REF = "ACG"},
\code{ALT = "A"} (deletion of \code{CG}) or \code{REF = "A"}, \code{ALT = "ACC"} (insertion
of \code{CC}).}

\item{ref_genome}{A BSgenome object or a character alias (e.g.
\code{"GRCh37"}, \code{"GRCh38"}, \code{"mm10"}).}

\item{trans_ranges}{Optional transcript-ranges \code{data.table}. If \code{NULL}
and \code{add_transcript_ranges = TRUE}, the shipped table is used when
available for \code{ref_genome}.}

\item{name_of_vcf}{Optional name for the VCF, used only in warning /
error messages.}

\item{suppress_discarded_variants_warnings}{If \code{TRUE}, do not warn when
variants that cannot be processed are discarded.}

\item{explain_indels}{If \code{0}, do not explain. If \code{1}, emit messages for
indels whose position was left-shifted by justification. If \code{2}, emit
messages for all indels.}

\item{context_width_multiplier}{Multiplier used to guess how much
flanking sequence (per side) is needed to categorize each indel.}

\item{add_transcript_ranges}{If \code{TRUE}, annotate transcript strand when a
transcript-ranges table is available.}
}
\value{
A list with:
\itemize{
\item \code{annotated.vcf}: the input VCF with new columns \code{seq.context},
\code{seq.context.width}, \code{pos_shift}, \code{COSMIC_83}, \code{Koh_89}, \code{Koh_476},
and (when transcript ranges are available) \code{trans.strand} /
\code{bothstrand}.
\item \code{discarded.variants}: rows that could not be justified, or \code{NULL} if
none were discarded.
}
}
\description{
Port of ICAMS's \code{AnnotateIDVCF()}. Justifies each indel against the
reference genome, optionally annotates transcript strand, and categorizes
each justified indel into the COSMIC 83, Koh 89, and Koh 476 schemes.
}
