% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_sbs_or_dbs_vcf.R
\name{annotate_sbs_or_dbs_vcf}
\alias{annotate_sbs_or_dbs_vcf}
\title{Annotate an SBS or DBS VCF with flanking sequence context and transcript
strand}
\usage{
annotate_sbs_or_dbs_vcf(
  vcf,
  ref_genome,
  trans_ranges = NULL,
  seq_context_width = 10L,
  name_of_vcf = NULL
)
}
\arguments{
\item{vcf}{A VCF as a data.frame / data.table with \code{CHROM}, \code{POS},
\code{REF}, \code{ALT} columns. For SBS, \code{REF} and \code{ALT} are single bases; for
DBS, both are two bases.}

\item{ref_genome}{A BSgenome object or a character alias accepted by
\code{\link[=normalize_genome_arg]{normalize_genome_arg()}}.}

\item{trans_ranges}{Optional transcript-ranges \code{data.table}. If \code{NULL},
the shipped table is used when available for \code{ref_genome}.}

\item{seq_context_width}{Width (per side) of the flanking-sequence
window (default 10 → 21-base window).}

\item{name_of_vcf}{Optional VCF name used in warnings.}
}
\value{
A list with:
\itemize{
\item \code{annotated.vcf}: the input VCF with new columns \verb{seq.<N>bases} and
(when transcript ranges are available) \code{trans.strand} /
\code{bothstrand}.
\item \code{discarded.variants}: currently always \code{NULL} for this path
(included for symmetry with \code{\link[=annotate_id_vcf]{annotate_id_vcf()}}).
}
}
\description{
Adds flanking sequence context via \code{\link[=add_seq_context]{add_seq_context()}} and, when a
transcript-ranges table is available, transcript strand via
\code{\link[=add_transcript_strand]{add_transcript_strand()}}. The same pipeline is appropriate for SBS and
DBS; DBS-specific validation (e.g. no \code{N} in the tetranucleotide
context) happens downstream in \code{\link[=vcf_to_catalog]{vcf_to_catalog()}}.
}
