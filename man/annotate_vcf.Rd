% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_vcf.R
\name{annotate_vcf}
\alias{annotate_vcf}
\title{Annotate a VCF with flanking sequence context and transcript strand}
\usage{
annotate_vcf(
  vcf,
  ref_genome,
  variant_type = c("auto", "SBS", "DBS", "ID"),
  trans_ranges = NULL,
  seq_context_width = 10L,
  name_of_vcf = NULL
)
}
\arguments{
\item{vcf}{A VCF as a data.frame / data.table with \code{CHROM}, \code{POS},
\code{REF}, \code{ALT} columns.}

\item{ref_genome}{A BSgenome object or a character alias accepted by
\code{\link[=normalize_genome_arg]{normalize_genome_arg()}}.}

\item{variant_type}{One of \code{"auto"}, \code{"SBS"}, \code{"DBS"}, \code{"ID"}. For
\code{"auto"}, rows are routed by REF/ALT length.}

\item{trans_ranges}{Optional transcript-ranges \code{data.table}. If \code{NULL},
the shipped table is used when available.}

\item{seq_context_width}{Width (per side) of the flanking-sequence
window; default 10 (produces a 21-base window).}

\item{name_of_vcf}{Optional VCF name used in warnings.}
}
\value{
A \code{data.table} with the annotation columns added.
}
\description{
\code{annotate_vcf()} is the high-level annotator used by
\code{\link[=vcf_to_catalog]{vcf_to_catalog()}}. It dispatches on \code{variant_type}:
}
\details{
\itemize{
\item \code{"SBS"}: add flanking sequence context (\code{\link[=add_seq_context]{add_seq_context()}}) and
transcript strand (\code{\link[=add_transcript_strand]{add_transcript_strand()}} if \code{trans_ranges} is
supplied or shipped for \code{ref_genome}).
\item \code{"DBS"}: same as SBS; downstream \code{\link[=vcf_to_catalog]{vcf_to_catalog()}} will require the
tetranucleotide context to be free of \code{N}.
\item \code{"ID"}: placeholder — indel annotation will require indel
justification and repeat-context detection. Not yet implemented in
this port.
\item \code{"auto"}: classify each row by REF/ALT length and annotate each
subset appropriately. Only the SBS / DBS paths are wired up for now.
}
}
