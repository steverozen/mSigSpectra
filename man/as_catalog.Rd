% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog_attr.R
\name{as_catalog}
\alias{as_catalog}
\title{Turn a numeric matrix into a mutational-spectrum catalog}
\usage{
as_catalog(
  x,
  type = NULL,
  ref_genome = NULL,
  region = "unknown",
  abundance = NULL,
  counts_or_density = "counts",
  infer_rownames = FALSE
)
}
\arguments{
\item{x}{A numeric matrix, data.frame coercible to numeric, or a named
numeric vector (converted to a one-column matrix with the vector names
as rownames).}

\item{type}{Optional catalog type identifier (\code{"SBS96"}, \code{"DBS78"},
\code{"ID83"}, etc.). Inferred from \code{nrow(x)} if \code{NULL}.}

\item{ref_genome}{Optional BSgenome object or alias; recorded as an
attribute and used for abundance lookup.}

\item{region}{One of \code{"genome"}, \code{"exome"}, \code{"transcript"},
\code{"unknown"}. For stranded catalogs (\code{SBS192}, \code{DBS144}) \code{"genome"} is
silently promoted to \code{"transcript"}.}

\item{abundance}{Optional named numeric vector of k-mer counts. If
\code{NULL}, inferred from shipped \code{all.abundance} keyed on \code{ref_genome}
and \code{region}.}

\item{counts_or_density}{One of \code{"counts"}, \code{"density"},
\code{"counts.signature"}, \code{"density.signature"}.}

\item{infer_rownames}{If \code{TRUE} and \code{x} has no rownames, the canonical
rownames for the catalog type are attached (\strong{assuming the row order
is already correct}). If \code{FALSE}, \code{x} must already have the canonical
rownames.}
}
\value{
\code{x} as a numeric matrix with attributes set.
}
\description{
Attaches the standard catalog attributes (\code{type}, \code{counts_or_density},
\code{ref_genome}, \code{region}, \code{abundance}) to \code{x} and returns it. \strong{No S3
class is set} — mSigSpectra catalogs are plain matrices with
attributes; functions key off \code{attr(x, "type")} via explicit checks.
}
\examples{
m <- matrix(
  1, nrow = 96, ncol = 1,
  dimnames = list(catalog.row.order$SBS96, "sample1")
)
cat96 <- as_catalog(m)
attr(cat96, "type")   # "SBS96"
attr(cat96, "region") # "unknown"

}
