% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_one_file_rosetta.R
\name{build_one_file_rosetta}
\alias{build_one_file_rosetta}
\title{Build a one-file indel "Rosetta stone" Excel table}
\usage{
build_one_file_rosetta(
  vcf_path,
  out_path = NULL,
  flank_5 = 5,
  flank_3 = 20,
  cap_9 = TRUE,
  show_details = FALSE,
  one_singletc = FALSE,
  n_examples = 20,
  sort_by_count = FALSE
)
}
\arguments{
\item{vcf_path}{Path to one annotated indel VCF (may be gzipped). Must
contain the columns \code{Koh_476}, \code{Koh_89}, \code{COSMIC_83}, and \code{long_visual}.}

\item{out_path}{Path of the \code{.xlsx} file to write. If \code{NULL}, no file is
written and only the table is returned.}

\item{flank_5}{Number of bases to keep from the 5' flank in \code{long_visual}.}

\item{flank_3}{Number of bases to keep from the 3' flank in \code{long_visual}.}

\item{cap_9}{If \code{TRUE}, drop single-base C/T indel rows whose repeat count
is 10 or more (no counterpart in the cap-9 ID476/ID89 mapping) and rewrite
\code{R9} as \code{R(9,)}.}

\item{show_details}{If \code{TRUE}, include the annotation detail columns after
"Indel in context".}

\item{one_singletc}{If \code{TRUE}, show 1 example per single-base C/T class
instead of 5.}

\item{n_examples}{Maximum number of examples per (\code{Koh_476}, \code{Koh_89}) pair
for classes other than single-base C/T indels.}

\item{sort_by_count}{If \code{TRUE}, order the (\code{Koh_476}, \code{Koh_89}) blocks by
descending \code{n_indels} (the number of indels in the input VCF with that
pair) instead of by the canonical ID476 row order. LibreOffice and Excel
cannot sort a range containing merged cells, so this is the way to get
a count-sorted table.}
}
\value{
Invisibly, a \code{data.table} with the rows written to the workbook.
}
\description{
Reads a single annotated indel VCF (as written by \code{\link[=annotate_id_vcf]{annotate_id_vcf()}}) and
writes an Excel workbook that links the \code{Koh_476}, \code{Koh_89}, and
\code{COSMIC_83} classifications, with example indels in sequence context for
each observed (\code{Koh_476}, \code{Koh_89}) pair. This is a single-file version of
the three-step pipeline that built Supplementary Table S1 of Liu et al.
(2026), with the steps combined into one function.
}
