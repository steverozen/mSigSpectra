% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discarded_variants.R
\name{check_and_remove_discarded_variants}
\alias{check_and_remove_discarded_variants}
\title{Check a VCF for common variant-level problems and remove the offenders}
\usage{
check_and_remove_discarded_variants(
  vcf,
  name_of_vcf = NULL,
  chr_names_to_process = NULL
)
}
\arguments{
\item{vcf}{A VCF as a data.frame / data.table.}

\item{name_of_vcf}{Optional name, used in warning messages.}

\item{chr_names_to_process}{Optional character vector of chromosome
names to keep (overrides the default non-standard-contig filter).}
}
\description{
Removes:
\itemize{
\item Rows with identical REF and ALT.
\item Stray \verb{#CHROM} header repeats.
\item Duplicated (CHROM, POS, REF, ALT) rows (keeping one copy).
\item Multiple-ALT rows (comma-separated ALT field).
\item Non-standard chromosome names (or those outside
\code{chr_names_to_process} when supplied).
\item Substitutions of length > 2 (e.g. \code{ACT>TGA}).
\item Complex indels (\code{REF[1] != ALT[1]}).
\item Wrong DBS rows where REF and ALT share a base at the same position.
\item Variants whose REF base is not in \verb{\{A, C, G, T\}}.
}
}
\details{
Each discarded row gains a \code{discarded.reason} column. Returns a list
with \code{df} (retained rows) and optionally \code{discarded.variants}
(discarded rows).
}
