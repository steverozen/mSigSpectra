% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog_io.R
\name{read_catalog}
\alias{read_catalog}
\title{Read a mutational-spectrum catalog from a file}
\usage{
read_catalog(
  file,
  ref_genome = NULL,
  region = "unknown",
  counts_or_density = "counts",
  format = c("auto", "ICAMS", "SigProfiler", "COSMIC")
)
}
\arguments{
\item{file}{Path to the catalog file.}

\item{ref_genome}{Optional BSgenome object or alias; stored as attribute.}

\item{region}{One of \code{"genome"}, \code{"exome"}, \code{"transcript"},
\code{"unknown"}.}

\item{counts_or_density}{One of \code{"counts"}, \code{"density"},
\code{"counts.signature"}, \code{"density.signature"}.}

\item{format}{\code{"auto"} (default), \code{"ICAMS"}, \code{"SigProfiler"}, or
\code{"COSMIC"}. In \code{"auto"} mode the format is inferred from the file's
first data row.}
}
\value{
A catalog matrix with attributes (see \code{\link[=as_catalog]{as_catalog()}}).
}
\description{
Auto-detects the file format from its first row. Recognized formats:
\itemize{
\item \strong{ICAMS native} CSV — first N columns are header columns (mutation type,
context, etc), remaining columns are per-sample counts / densities.
\item \strong{SigProfiler} TSV / CSV — single header column with bracketed mutation
labels like \verb{A[C>A]A}, \verb{AA[C>A]AA}, or PCAWG indel codes like
\code{1:Del:C:0}.
\item \strong{COSMIC} CSV — SBS96 uses the ICAMS-external row-header format;
SBS192 uses a stranded variant of the same; DBS78 / ID83 also share
their SigProfiler-style layout.
}
}
\details{
The matrix is reordered to the canonical rownames for its type and
wrapped in a catalog via \code{\link[=as_catalog]{as_catalog()}}.
}
