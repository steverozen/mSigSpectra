% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_vcf.R
\name{read_vcf}
\alias{read_vcf}
\title{Read a VCF file into a data.table, caller-agnostically}
\usage{
read_vcf(
  file,
  filter = TRUE,
  backend = c("fread", "vcfppR"),
  name_of_vcf = NULL
)
}
\arguments{
\item{file}{Path or URL to the VCF file.}

\item{filter}{Controls which rows are kept based on the \code{FILTER} column:
\itemize{
\item \code{TRUE} (default): keep rows where \code{FILTER \%in\% c("PASS", ".", "")} —
the union of passing values across common callers.
\item \code{FALSE} or \code{NULL}: keep all rows.
\item A character vector: keep rows where \code{FILTER \%in\% filter}.
Rows with no \code{FILTER} column are always kept; a warning is emitted
when \code{filter} is non-trivial in that case.
}}

\item{backend}{VCF reader backend:
\itemize{
\item \code{"fread"} (default): \code{\link[data.table:fread]{data.table::fread()}}. Handles uncompressed and
gzipped files; does not handle bgzipped/tabix.
\item \code{"vcfppR"}: \code{\link[vcfppR:vcftable]{vcfppR::vcftable()}} — handles bgzipped, tabix, and
remote files; splits multi-allelic records. The \code{vcfppR} package is
in \code{Suggests}; an informative error is raised if it is not installed.
}}

\item{name_of_vcf}{Optional name for the VCF, used only for warning /
error messages. Defaults to the filename with extension stripped.}
}
\value{
A \code{data.table} with one row per variant.
}
\description{
Reads the \emph{body} of a VCF file (lines after \verb{#CHROM}) into a \code{data.table}.
The resulting table has whatever columns the VCF has (\code{CHROM}, \code{POS}, \code{ID},
\code{REF}, \code{ALT}, \code{QUAL}, \code{FILTER}, \code{INFO}, and optionally \code{FORMAT} plus one
or more sample columns), with \verb{#CHROM} renamed to \code{CHROM}.
}
\details{
\strong{Caller-agnostic.} \code{read_vcf()} does not know or care which variant
caller produced the VCF. It does not parse \code{FORMAT}/sample columns and
does not extract VAF or read depth. The only caller-dependent semantics
is the default value of the \code{filter} argument (see below).
}
