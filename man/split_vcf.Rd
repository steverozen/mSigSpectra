% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_vcf.R
\name{split_vcf}
\alias{split_vcf}
\title{Split a mixed-mutation VCF into SBS / DBS / ID sub-tables}
\usage{
split_vcf(vcf, name_of_vcf = NULL)
}
\arguments{
\item{vcf}{A VCF as a data.frame / data.table with at least \code{REF} and
\code{ALT} columns.}

\item{name_of_vcf}{Optional VCF name used in warning / error messages.}
}
\value{
A list with elements
\itemize{
\item \code{SBS}: \code{data.table} of SBS rows.
\item \code{DBS}: \code{data.table} of DBS rows.
\item \code{ID}: \code{data.table} of indel rows.
\item \code{discarded}: \code{data.table} of rows that did not fit any of the above,
with a \code{discarded.reason} column — \strong{\code{NULL} when no rows were
discarded}.
}
}
\description{
Classifies each row by \code{REF}/\code{ALT} length alone:
\itemize{
\item SBS (single base substitution): \code{nchar(REF) == 1 && nchar(ALT) == 1}
\item DBS (double base substitution): \code{nchar(REF) == 2 && nchar(ALT) == 2}
\item ID (indel):                     \code{nchar(REF) != nchar(ALT)}
\item Other (e.g. 3+bp substitutions): discarded with a recorded reason.
}
}
\details{
This is \strong{caller-agnostic}. In particular, mSigSpectra does \emph{not}
merge adjacent SBSs into DBSs based on VAF similarity (which ICAMS did
via \code{SplitOneVCF} for Strelka-style VCFs). Users who want that behavior
should apply it as a post-processing step with their own VAF column.
}
