% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog_transform.R
\name{transform_catalog}
\alias{transform_catalog}
\title{Transform a catalog between counts and density}
\usage{
transform_catalog(
  catalog,
  target_ref_genome = NULL,
  target_region = NULL,
  target_counts_or_density = NULL,
  target_abundance = NULL
)
}
\arguments{
\item{catalog}{An mSigSpectra catalog (see \code{\link[=as_catalog]{as_catalog()}}) with
attributes \code{type}, \code{counts_or_density}, \code{ref_genome}, \code{region},
\code{abundance}.}

\item{target_ref_genome, target_region, target_counts_or_density}{Target
attributes. If \code{NULL}, the catalog's current attribute is used.}

\item{target_abundance}{Optional target abundance vector. If \code{NULL},
inferred from \code{target_ref_genome} + \code{target_region}.}
}
\value{
A new catalog (plain matrix with attributes).
}
\description{
Converts between counts (raw mutation counts per category) and density
(mutations per megabase of context) representations, and between
catalog ↔ signature (column-normalized) forms. The transformation is
expressed by multiplying each category's count by
\code{target_abundance[source.n.mer] / source_abundance[source.n.mer]},
where \code{source.n.mer} is the reference context encoded in the row name
(first 3 characters for SBS96/192, 5 for SBS1536, 2 for DBS78/144, 4
for DBS136).
}
