% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog_build.R
\name{vcf_to_catalog}
\alias{vcf_to_catalog}
\title{Build a mutational-spectrum catalog from an annotated VCF}
\usage{
vcf_to_catalog(
  annotated_vcf,
  type = c("SBS96", "SBS192", "SBS1536", "DBS78", "DBS136", "DBS144", "ID83", "ID89",
    "ID166", "ID476"),
  ref_genome = NULL,
  region = "unknown",
  sample_name = "count"
)
}
\arguments{
\item{annotated_vcf}{A VCF annotated by \code{\link[=annotate_sbs_or_dbs_vcf]{annotate_sbs_or_dbs_vcf()}}
(for SBS / DBS types) or \code{\link[=annotate_id_vcf]{annotate_id_vcf()}} (for ID types). May be
the bare annotated \code{data.table} or the full
\code{list(annotated.vcf, discarded.variants)} returned by either
annotator. For SBS types the table must contain a \verb{seq.<N>bases}
column and (for SBS192) \code{trans.strand} / \code{bothstrand}.}

\item{type}{Catalog type identifier (one of \code{"SBS96"}, \code{"SBS192"},
\code{"SBS1536"}, \code{"DBS78"}, \code{"DBS136"}, \code{"DBS144"}, \code{"ID83"}, \code{"ID89"},
\code{"ID166"}, \code{"ID476"}).}

\item{ref_genome}{Optional BSgenome object or alias; recorded on the
output catalog.}

\item{region}{One of \code{"genome"}, \code{"exome"}, \code{"transcript"},
\code{"unknown"}.}

\item{sample_name}{Column name for the single-sample catalog matrix
(default \code{"count"}).}
}
\value{
A single-column numeric matrix with catalog attributes (see
\code{\link[=as_catalog]{as_catalog()}}).
}
\description{
Dispatches to a type-specific internal builder based on \code{type}.
Supports SBS96 / SBS192 / SBS1536 today; DBS and ID catalog types are
stubs pending the DBS- and indel-specific ports.
}
