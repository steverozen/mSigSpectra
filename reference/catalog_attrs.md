# Report the attributes of an mSigSpectra catalog

Report the attributes of an mSigSpectra catalog

## Usage

``` r
catalog_attrs(x)
```

## Arguments

- x:

  A catalog.

## Value

A named list with elements `type`, `counts_or_density`, `ref_genome`,
`region`, `abundance`.
