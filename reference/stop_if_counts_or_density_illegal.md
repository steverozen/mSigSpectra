# Validate a counts_or_density argument

Validate a counts_or_density argument

## Usage

``` r
stop_if_counts_or_density_illegal(counts_or_density)
```

## Value

`NULL`, invisibly. Called for its side effect of raising an error when
`counts_or_density` is not a legal value.
